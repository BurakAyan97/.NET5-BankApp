@model SelectList
@{
    ViewData["Title"] = "SendMoney";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@*
    SenderId,
    AccountId,
    Amount
*@
<div class="mt-3">
    <form asp-action="SendMoney" method="post" class="w-75 mx-auto">
        <input type="hidden" name="SenderId" value="@ViewBag.SenderId" />
        <div class="mb-3">
            <label>Göndereceğiniz Hesap Numarası:</label>
            <select class="form-select" asp-items="@Model" name="AccountId">
            </select>
        </div>
        <div class="mb-3">
            <label>Göndereceğiniz Tutar:</label>
            <input type="number" name="Amount" class="form-control" />
        </div>
        <div class="mb-3">
            <button class="btn btn-outline-primary" type="submit">Para Gönder</button>
        </div>
    </form>

</div>

